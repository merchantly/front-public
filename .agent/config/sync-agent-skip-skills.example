# Copy to sync-agent-skip-skills — one skill directory name per line (basename under skills/).
# Use for skills installed only via external installers so sync-agent.sh does not overwrite
# .cursor/.claude symlinks with links into .agent/skills/.
#
# example-skill-from-package
